package quarta.aula.enuns;

public class DiasSemanaPrograma {

    public static void main(String[] args) {
        DiasSemana diasSemana = DiasSemana.DOMINGO;

        int entradaUsuario = 1;
        String dia = "Domingo";

        if(entradaUsuario == DiasSemana.DOMINGO.getId());
            if (dia.equals(DiasSemana.DOMINGO.getDescricao()))
        System.out.println(diasSemana.getDescricao());
        System.out.println(diasSemana.getId());
        System.out.println(diasSemana);
    }
}
